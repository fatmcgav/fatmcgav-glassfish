# File managed by Puppet
if ! echo ${PATH} | /bin/grep -q <%= scope.lookupvar('glassfish::glassfish_dir') %>/glassfish/bin ; then
        PATH=<%= scope.lookupvar('glassfish::glassfish_dir') %>/glassfish/bin:${PATH}
fi
